# In-App Purchase Configuration
# Set to "true" to enable subscription checks (paywall gating)
# Set to "false" or omit during development to bypass subscription checks
EXPO_PUBLIC_PAYWALL_ENABLED=false
